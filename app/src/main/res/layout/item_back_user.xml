<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/space_medium"
    android:background="@color/grayLight"
    android:padding="@dimen/space_big">

    <ImageView
        android:id="@+id/ivItemBackUser"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_user" />

    <TextView
        android:id="@+id/tvItemUserSurname"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="EMA development"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/tvItemUserName"
        app:layout_constraintTop_toBottomOf="@+id/tvItemUserName" />

    <TextView
        android:id="@+id/tvItemUserName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/space_big"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/tvItemUserSurname"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@id/ivItemBackUser"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Carmabs" />
</androidx.constraintlayout.widget.ConstraintLayout>